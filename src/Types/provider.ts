export type Provider = {
  id: string;
  providerName: string;
  providerCode: string;
  providerId: string;
  providerType: string;
  jobSource: string;
  vendorSource: string;
  accountId: string;
};
